TROUVER UN BINOME !!!

leaks:
delete data opengl:
	Skybox
	Blueprint
	Program
-----------------------------------------------------------------
class Obj (cf Obj3d.hpp)
class Obj3d:
	some default values should be private
	_displayTexture : setter ? check if a Texture is linked, return -1/0
		a check is made in the rendering program, so its useless (obj3dPG::render())
Parent system:
	Obj &			parent;
	Math::Matrix4	worldModelMatrix; // = parent.getWorldModelMatrix(); then mult(localModelMatrix); care of outdated matrices!
	Math::Matrix4	localModelMatrix;
	local pos/rot/scale ?
motion behavior:
	Obj: set a pointer to a user defined func that will describe his motion (looped)
	on/off
	ex: automatic rotate on Yaxis
rescaled obj(3d):
	set a default size (say 5), then scale the oject to that, using the largest size min->max (X or Y or Z)
	Vector3 rescale; should NEVER change once defined!
	final scale = scale * rescale
	add a setSize() func: that modifys the scale with the largest size min->max XYZ to reach the desired size
quaternions: https://www.youtube.com/watch?v=d4EgbgTm0Bg

-----------------------------------------------------------------
GUI	Qt